<br/>

```csharp
new Date()              // 当前时间
new Date('2024-01-01')  // 字符串解析
new Date(2024, 0, 1)    // 年、月(0-11)、日
new Date(1704067200000) // 时间戳
    
-------------------------------------
    
# 年月日
date.getFullYear()    // 获取年份 (2024)
date.getMonth()       // 获取月份 (0-11)
date.getDate()        // 获取日期 (1-31)
date.getDay()         // 获取星期几 (0-6, 0为周日)

# 时分秒
date.getHours()        // 获取小时 (0-23)
date.getMinutes()      // 获取分钟 (0-59)
date.getSeconds()      // 获取秒 (0-59)
date.getMilliseconds() // 获取毫秒 (0-999)
    
# [ 时间戳 ] 毫秒
const date = new Date();
console.log(date.getTime());
console.log(+ new Date());
console.log(Date.now());

# [ 设置日期 ]
date.setFullYear(2025)
date.setMonth(11)     // 设置为12月
date.setDate(25)
date.setHours(10, 30, 0, 0)  // 时、分、秒、毫秒


```

>```csharp
># [ 自定义格式化 ]
>function formatDate(date, format) {
>    
>     const year = date.getFullYear();
>     const month = String(date.getMonth() + 1).padStart(2, '0');
>     const day = String(date.getDate()).padStart(2, '0');
>     const hours = String(date.getHours()).padStart(2, '0');
>     const minutes = String(date.getMinutes()).padStart(2, '0');
>     const seconds = String(date.getSeconds()).padStart(2, '0');
>
>     return format
>         .replace('YYYY', year)
>         .replace('MM', month)
>         .replace('DD', day)
>         .replace('HH', hours)
>         .replace('mm', minutes)
>         .replace('ss', seconds);
>     
>}
>
>// 2025-11-13 16:20:32
>const date = new Date();
>console.log(formatDate(date, 'YYYY-MM-DD HH:mm:ss'))
>    
>```
>
>
>
>



