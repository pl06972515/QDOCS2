<br/>

>[!WARNING|style: flat|label: 简要说明 ]
>
>- `bool`是一个内置的数据类型，用于表示布尔值 <span style='color:Blue'>[ 只有两个值`true`和`false`]</span>
>
> ```csharp
>let x = true;
> let y = false;
>
> 
>```
>
>
>
><br/>

```csharp
{
    
      # [ 显示转换 ] 以下为 false ( 其余都为 true )
      Boolean(false)     → false
      Boolean(0)         → false
      Boolean('')        → false
             
      Boolean(NaN)       → false
      Boolean(undefined) → false
      Boolean(null)      → false
      
}


```

