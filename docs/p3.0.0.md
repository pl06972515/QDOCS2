<br/>

>[!WARNING|style: flat|label: 简要说明 ]
>
>```csharp
>let M1 = function (x, y) {
>
>     let z = x + y
>     return z
>     
>}
>console.log(typeof M1) // function
>console.log(M1(1, 2))
>
>
>```
>
>
>
><br/>



<!-- tabs:start -->

#### **void**

!> <span style='color:red'>针对无返回值函数 [ 实际上其返回了`undefined`]</span>

```csharp
let M1 = function () {

    console.log('M1 function')
}

console.log(typeof M1) // function
console.log(M1())      // undefined


```



#### **[ 函数参数 ]**

>[!WARNING|style: flat|label: 简要说明 ]
>
>- 参数传递：[ 位置传参 ] [ 命名参数 ]
>
>- 剩余参数：[`JS`支持剩余参数`..args`] <span style='color:red'>[ 可以使用数组相关函数 ]</span>
>
><br/>

```csharp
let M1 = function (x, y = 10, ...args) {

     # [ object ]
     console.log(typeof args) 
     for (let [key, value] of Object.entries(args)) {

         // 0: 30
         // 1: 40
         // 2: 50
         console.log(`${key}: ${value}`);

     }
     console.log(args[0]);

}

M1(x = 10, 20, 30, 40, 50)


```



#### **[ 解包 ] return**

```csharp
function M1() {
    
    let n1 = 10;
    let n2 = 20;
    let n3 = 30;

    # [ 返回对象 ] 使用简化语法 ( ES6+ )
    return { n1, n2, n3 };
    
}

// 10, 20, 30 ( 不支持 _ )
const { n1, n2, n3 } = M1();
console.log(n1, n2, n3); 


```





#### **[ ℹ️ lambda ] 箭头函数**

>[!WARNING|style: flat|label: 简要说明 ]
>
>- 箭头函数属于表达式函数(`lambda`) 
>
>  <span style='color:red'>[ 箭头函数特殊，不绑定`this`: 因此，箭头函数内`this`指向上一级作用域的`this`]</span> [<span style='color:#008B00'>[👓 Q1 ]</span>](F2.0.0  ':target=_blank')
>
>  <span style='color:Blue'>[ 适用于：匿名函数的地方 ]</span>
>
><br/>

```csharp
// [ Action ]
const M1 = () => print("Hello, world!")
M1()

// [ Action<int, int> ]
const M1 = (x, y=30, ...args) => print(x + y)
M1(10)

------------------------------------
    
// [ Func<int> ]
const M1 = () => 100
x = M1()

// [ Func<int, int, int> ]
const M1 = (x, y=30) => x + y
x = M1(10)


```



#### **[ ℹ️ 立即执行 ]**

!> <span style='color:red'>[ 函数定义后马上执行，不需要单独调用 ]</span>

```csharp
(function(x, y){

    console.log("This is a test file.");

})(10, 20)


```

```csharp
!function (x, y) {

    console.log("This is a test file.");

}(10, 20)


```





#### **[ ✡ 回调函数 ]**

```csharp
function M1() {
    return 'M1';
}

# callback: 回调函数 ( C# 委托 )
function Main(callback) {
    return callback();
}

const v = Main(M1);
console.log(v); // Should print 'M1'


```



<!-- tabs:end -->





