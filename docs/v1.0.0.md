<br/>

>[!WARNING|style: flat|label: 简要说明 ]
>
>- 数值类型包含：<span style='color:Blue'>[ 整数`int`] [ 浮点数`float`]</span>
>
>   <span style='color:red'>[ 特殊值`NaN`、`Infinity`、`-Infinity`]</span>
>
>   
>   <span style='color:red'>[`NaN`是`JavaScript`中的一个特殊数值 - 表示：不是数字 ]</span>
>   
>
>
>```javascript
># [ 底层机制 ] 把基本数据类型 → 包装成引用数据类型 new Number(10)
>let x = 10;          
>console.info(typeof x); // "number"
>let y = 10.5
>console.info(typeof y); // "number"
>let z = NaN;
>console.info(typeof z); // "number"
>
>console.log('' -1);           → -1
>console.log('Hello' - 1);     → NaN
>
>console.log(null + 1);        → 1
>console.log(undefined + 1);   → NaN
>console.log(NaN + 1);         → NaN
>
>
>```
>
>
>
><br/>

```csharp
{
    
     console.log(Number("14"));    
     console.log(Number("14.35"));     
     console.log(Number("sss"));   // NaN
    
     # [ 简化转换 ] Number( ... )
     +"123"        // 等同于 Number("123")
     +"123.345"    // 等同于 Number("123.345")
     +true         // 等同于 Number(true) → 1
     +false        // 等同于 Number(false) → 0
         
     +null         // 等同于 Number(null) → 0
     +""           // 等同于 Number("") → 0
     +undefined    // 等同于 Number(undefined) → NaN
     +"abc"        // 等同于 Number("abc") → NaN
    
     console.log(parseInt("123.45"));   // 转换整数: 123    ( 转换失败：NaN )
     console.log(parseFloat("123.45")); // 转浮点数: 123.45 ( 转换失败：NaN )
    
     # [ 数值检测 ]
     console.log(isNaN("abc"));    // true
     console.log(isNaN("123"));    // false
     console.log(isNaN("123.45")); // false
     
     console.log(Number.isFinite(true));  // false ( 推荐: 排除 NaN、Infinity、-Infinity、'123'字符串 + bool 不会被转换 )
     console.log(Number.isInteger(4.0));  // 是否为整数: true
     
     // [ 检测 ] 是否为一个数字
     function isNumeric(val) {
         return typeof val === 'number'
             && !isNaN(val)
             && isFinite(val)
             && val >= Number.MIN_SAFE_INTEGER
             && val <= Number.MAX_SAFE_INTEGER;
     }
     
     # [ 数值格式化 string ] 要求被操作对象为 Number ( 四舍五入 )
     let num = 123.456;
     console.log(num.toFixed());    // "124"    ( 四舍五入 )
     console.log(num.toFixed(1));   // "123.6"  ( 四舍五入 )
     console.log(num.toFixed(2));   // "123.57" ( 四舍五入 )
     console.log(num.toString());   // "123.567"
    
}


```

```csharp
{
    
     # [ lodash ] 第三方包
    
     // value: NaN | null | undefined → 返回默认值
     _.defaultTo(undefined, 10);
         
}


```

